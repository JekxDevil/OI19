/*
first line is a set of pairs to exclude when near
the output is built in a stack.
the next lines are to be filtered.
*/
#include <ctype.h>  // ispace()
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#define MAX_LEN 1000

char stack[MAX_LEN + 1];
int main(int argc, char* argv[]) {
    const char* DEFAULT = "()[]{}";
    int open = 1;
    while (1) {
        continue;
    }
}
